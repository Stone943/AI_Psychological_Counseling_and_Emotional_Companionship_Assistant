# Demo deployment inputs — paths/digests only; never place secret values here.
DEMO_DOMAIN=demo.example.invalid
ENCRYPTION_KEY_FILE=/path/to/encryption.key
JWT_SECRET_FILE=/path/to/jwt_secret
REFRESH_SECRET_FILE=/path/to/refresh_secret
DATABASE_URL_FILE=/path/to/database_url
MYSQL_ROOT_PASSWORD_FILE=/path/to/mysql_root_password
MYSQL_PASSWORD_FILE=/path/to/mysql_password
CADDY_IMAGE_DIGEST=sha256:replace-with-reviewed-digest
MYSQL_IMAGE_DIGEST=sha256:replace-with-reviewed-digest
REDIS_IMAGE_DIGEST=sha256:replace-with-reviewed-digest
PYTHON_IMAGE_DIGEST=sha256:replace-with-reviewed-digest
UV_IMAGE_DIGEST=sha256:replace-with-reviewed-digest
